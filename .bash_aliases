# Ask before removing (does not work if -f is specified).
alias rm='rm -i'
# Ask for confirmation before overwriting an existing file.
alias cp='cp -i'
alias mv='mv -i'

# List top to bottom and present index.
alias dirs='dirs -v'
# Tells man how wide to format its output for easy reading.
alias man='MANWIDTH=80 man'
